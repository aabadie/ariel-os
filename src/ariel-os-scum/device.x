/* SCuM interrupt handlers.
 *
 * Placeholder until the interrupt vector table is populated (milestone 2:
 * the SCuM SDK C handlers, most importantly OPTICAL_SFD_Handler, get wired
 * to the cortex-m-rt device vector table here).
 */
